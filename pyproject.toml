[project]
name = "xenbase-mcp"
version = "0.1.0"
description = "Xenbase MCP server for GitHub Copilot / VS Code"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.0,<3",
    "httpx>=0.27,<1",
]

[project.scripts]
xenbase-mcp = "xenbase_mcp.server:main"

[tool.uv]
package = true
